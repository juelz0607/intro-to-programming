# What will the following code print to the screen?

def scream(words)
  words = words + "!!!!"
  return
  puts words
end

scream("Yippeee")


--- Nothing will be printed to the screen, because all code after "return" 
will not be executed. 